export interface Envision {
  label: string,
  value: boolean
}

export interface NavigationLink {
  link: string,
  label: string,
  direction?: string | undefined
}

export interface Experience {
  label: string,
  value: boolean
}